{
  "name": "mailchimpapi",
  "version": "1.0.0",
  "description": "Sample code to store leads in mailchimp server using post requests",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "som",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
